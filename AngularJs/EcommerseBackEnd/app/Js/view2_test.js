'use strict';

describe('myApp.Views module', function() {

  beforeEach(module('myApp.Views'));

  describe('Views controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('ProductCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});